#ifndef language_ast_tree_hpp
#define language_ast_tree_hpp

#include "language/ast.hpp"

// -- A S  N A M E S P A C E --------------------------------------------------

namespace as {


	class tree final {

		private:

			//std::vector<as::unique<as::assignment>> _assigns;
			//std::vector<as::unique<as::node>> _exprs;

		public:


			//auto add_assignment(as::unique<as::assignment>&& n) noexcept -> void {
			//	_assigns.emplace_back(std::move(n));
			//}
			//auto add_expression(as::unique<as::node>&& n) noexcept -> void {
			//	_exprs.emplace_back(std::move(n));
			//}

	}; // class tree

} // namespace as

#endif // language_ast_tree_hpp
