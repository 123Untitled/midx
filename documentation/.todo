
# -------------
array of trigger
+ array of step before next trigger.
or only one array contains struct with

struct {
	bool trig;
	uint step_to_next;
};


# -----------
.scale
implement lookup table for scale like relative jumps
.scale C minor
jump[127] = {0, -1, 0, 0, -1, 0, -1, 0, 0, -1, 0, -1}
but how to choose + or - ?
imlement many modes like: follow mouvement, go down, go up, random, etc.


# -----------
change comment symbol + accept Cb Fb or B# E# as valid note names


# -----------
implement panic parameter 



# --------
nvim match parent ()[]{}   disable <>

#------
leader+space->play stop

#------
vim midi note live selector listen



# binary number is broken !!


#------
add random step param: 0 or 1 to enable/disable random step selection :rd 0 0 (tr | nt) 0 vl
add random value param ?


# ---- 
operator <+> positive superposition (AND logic)
operator <-> negative superposition (NAND logic)
operator <^> exclusive superposition (XOR logic)
operator <=> equivalence superposition (XNOR logic)
